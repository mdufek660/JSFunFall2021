/**
 * Double the numbers in an array.
 * This is similar to the previous exercise, except you are going to solve it within a function.
 * @param {array} numbers an array of numbers
 * @returns {array} a new array, where each number in the array is double of the "numbers" array
 *
 * @example
 * const numbers = [1, 3, 5];
 * const newNumbers = doubleNumbers(numbers);
 * console.log(newNumbers); // [2, 6, 10]
 */

const doubleValuesFunc = (numbers) => {
  // WRITE YOUR ANSWER HERE
};

// IGNORE THIS BELOW. It is for the tests.

export default doubleValuesFunc;
