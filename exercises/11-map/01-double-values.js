let newNumbers;
const numbers = [1, 3, 5]; // e.g.

/**
 * Using ".map()", double each number in the array "numbers"
 * and set "newNumbers" equal to the results of "map".
 *
 * @example
 * If "numbers" is [1, 3, 5]
 * "newNumbers" should be [2, 6, 10]
 *
 * You answer should still work if there are different numbers within the "numbers" array.
 */

// WRITE YOUR ANSWER BELOW THIS LINE

newNumbers = numbers.map(n=>{return n*2})